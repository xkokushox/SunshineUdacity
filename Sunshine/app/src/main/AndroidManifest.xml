<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.freakybyte.sunshine">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".SunshineApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ForecastTheme">
        <activity
            android:name=".controller.ui.activity.MainActivity"
            android:label=""
            android:theme="@style/ActionBar.Solid.Sunshine.NoTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.ui.activity.DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName=".controller.ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.ui.activity.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".controller.ui.activity.MainActivity">
        </activity>

        <provider
            android:name=".data.provider.WeatherProvider"
            android:authorities="com.freakybyte.sunshine"/>
    </application>

</manifest>